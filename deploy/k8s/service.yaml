apiVersion: v1
kind: Service
metadata:
  name: gitlab-mcp
  labels:
    app: gitlab-mcp
spec:
  selector:
    app: gitlab-mcp
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
